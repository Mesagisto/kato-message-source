rootProject.name = "mesagisto"
pluginManagement {
   repositories {
      maven("./maven-repo")
      gradlePluginPortal()
      mavenCentral()
      google()
      maven("https://jitpack.io")
   }
}
